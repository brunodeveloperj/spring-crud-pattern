# spring-crud-pattern
A generic and extensible Spring Boot library that automates the creation of CRUD operations using abstract controllers, services, and repositories through Java Generics and Reflection.
